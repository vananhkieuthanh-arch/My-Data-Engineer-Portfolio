version: 2

sources: 
  - name: raw
    schema: raw
    tables:
      - name: weather_daily
        columns:
          - name: country_code
            tests:
              - not_null
          - name: observation_date
            tests:
              - not_null
        tests:
          - dbt_utils.unique_combination_of_columns:
              combination_of_columns:
                - country_code
                - observation_date